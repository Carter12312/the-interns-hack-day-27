rootProject.name = "the-interns-hack-day-27"

// Keep the mobile application self-contained while allowing Android Studio to
// open the repository root. The app remains an independent included build.
includeBuild("mobile-app")

